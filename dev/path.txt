"{{.pathname}}": {
      "{{.method}}": {
        "summary": "{{.summary}}",
        "description": "{{.description}}",
        "operationId": "{{.operationId}}",
        "responses": {
          {{.responses}}
        },
        "tags": [
          {{.tags}}
        ]
      }
    }